# Experiment runs

Each run directory typically contains:

```
<runId>/
  config.json
  event_log.jsonl
  replay_index.json
  snapshots/
    D1-AM.json
    ...
```

Keep one **gold** run for demo after Day 6; delete noisy intermediates.
